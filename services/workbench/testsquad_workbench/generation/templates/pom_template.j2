from playwright.sync_api import Page

{% for import_name in com_imports %}
from .{{ import_name }} import {{ import_name }}
{% endfor %}


class {{ class_name }}:
    """Page Object Model for {{ description }}.

    Generated by TestRadius Workbench.
    """

    def __init__(self, page: Page) -> None:
        self.page = page
{% for com in components %}
        self.{{ com.name }} = {{ com.class_name }}(page, page.locator("{{ com.selector }}"))
{% endfor %}

    def navigate(self) -> None:
        """Navigate to the page URL."""
        if hasattr(self, "_url"):
            self.page.goto(self._url)

    def is_loaded(self) -> bool:
        """Check if all components are loaded."""
        return all(
            comp.is_loaded()
            for comp in self.__dict__.values()
            if hasattr(comp, "is_loaded")
        )
