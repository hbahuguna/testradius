"""Tests for {{ description }}.

Generated by TestRadius Workbench.
"""

import pytest
from playwright.sync_api import Page


class Test{{ class_name }}:
    """Smoke and flow tests for {{ description }}."""

    def test_page_loads(self, page: Page) -> None:
        """Verify the page loads without errors."""
        pom = {{ class_name }}(page)
        pom.navigate()
        assert pom.is_loaded(), "Page should be loaded"

{% if components %}
    def test_component_visibility(self, page: Page) -> None:
        """Verify all components are visible."""
        pom = {{ class_name }}(page)
        pom.navigate()
{% for com in components %}
        assert pom.{{ com.name }}.is_loaded(), "{{ com.name }} should be visible"
{% endfor %}
{% endif %}

{% for com in components %}
    {% if com.has_test_actions %}
    def test_{{ com.name }}_interaction(self, page: Page) -> None:
        """Test interaction with {{ com.name }}."""
        pom = {{ class_name }}(page)
        pom.navigate()
        assert pom.{{ com.name }}.is_loaded()
        {% if com.action_hint %}{{ com.action_hint }}{% endif %}

    {% endif %}
{% endfor %}
