from playwright.sync_api import Page, Locator


class {{ class_name }}:
    """Component Object Model for {{ component_type }}.

    Generated by TestRadius Workbench.
    Confidence: {{ confidence }}
    """

    def __init__(self, page: Page, root: Locator | None = None) -> None:
        self.page = page
        self._root = root or page.locator("{{ root_selector }}")

    @property
    def root(self) -> Locator:
        return self._root

    def is_loaded(self) -> bool:
        """Check if the component is present and visible on the page."""
        try:
            return self._root.is_visible()
        except Exception:
            return False

{% for field in fields %}
    @property
    def {{ field.name }}(self) -> Locator:
        return self._root.locator("{{ field.selector_value }}")
{% endfor %}
