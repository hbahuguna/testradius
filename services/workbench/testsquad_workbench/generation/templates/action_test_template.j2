"""Automated tests for {{ suite_name }}.

Generated by TestRadius Workbench.
"""

import pytest
from playwright.sync_api import Page

from {{ suite_name }} import {{ suite_name }}


class Test{{ suite_name }}:
    """Test suite generated from user-defined components and actions."""

    def test_flow(self, page: Page) -> None:
        pom = {{ suite_name }}(page)
        pom.navigate()

{% for step in steps %}
        # {{ step.description }}
{% if step.condition %}
{% set cond_sel = step.condition.selector or step.selector %}
{% if step.condition.type == "element_visible" %}
        if pom.page.locator("{{ cond_sel }}").is_visible():
{% elif step.condition.type == "element_exists" %}
        if pom.page.locator("{{ cond_sel }}").count() > 0:
{% endif %}
{% if step.is_action %}
            pom.{{ step.method }}({{ step.value }})
{% else %}
            {{ step.code }}
{% endif %}
{% elif step.is_action %}
        pom.{{ step.method }}({{ step.value }})
{% else %}
        {{ step.code }}
{% endif %}
{% endfor %}
