[tool.poetry]
name = "testsquad-shared"
version = "0.1.0"
description = "TestSquad Shared Contracts"
authors = ["TestSquad Team"]

[tool.poetry.dependencies]
python = "^3.11"
pydantic = "^2.6.0"
sqlmodel = "^0.0.16"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
