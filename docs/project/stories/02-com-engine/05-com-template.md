# Story 2.5: Jinja2 COM Template

**Status**: Complete
**Epic**: 2 — COM Generation Engine
**Effort**: 2d

---

## Summary

Jinja2 template that renders a Python Component Object Model (COM) class from a `ComponentDescriptor`. The generated class includes typed locator properties for each child element, an `__init__` accepting a `Page` and optional root `Locator`, and an `is_loaded()` visibility check.

## Files Created

- `services/workbench/testsquad_workbench/generation/templates/com_template.j2` — COM Jinja2 template
- `services/workbench/testsquad_workbench/generation/templates/__init__.py` — Package init
- `services/workbench/testsquad_workbench/generation/template_engine.py` — `render_com()` function
- `services/workbench/tests/testsquad_workbench/generation/test_template_engine.py` — 5 tests
- `docs/project/stories/02-com-engine/05-com-template.md` — This file

## Generated Code Structure

```python
class LoginForm:
    """Component Object Model for LoginForm. Generated by TestRadius Workbench."""

    def __init__(self, page, root=None):
        self.page = page
        self._root = root or page.locator("[data-testid='login-form']")

    @property
    def root(self) -> Locator: ...

    def is_loaded(self) -> bool: ...

    @property
    def username_input(self) -> Locator:
        return self._root.locator("[name='email']")

    @property
    def password_input(self) -> Locator:
        return self._root.locator("[name='password']")

    @property
    def submit_button(self) -> Locator:
        return self._root.locator("[type='submit']")
```

## Verification

```bash
uv run pytest tests/testsquad_workbench/generation/test_template_engine.py -v
```

Expected: 5 passed
